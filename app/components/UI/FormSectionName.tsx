export default function FormSectionName({ title }: { title: string }) {
  return (
    <div className="bg-blue text-center py-3 px-6 text-sm w-max text-white rounded-t-md">
      {title}
    </div>
  );
}
